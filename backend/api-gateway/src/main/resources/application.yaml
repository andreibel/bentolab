spring:
    application:
        name: api-gateway

server:
    port: 8080

jwt:
    secret: ${JWT_SECRET:PKr8VmsFMreD//KrCWf+kINmoKwKaSoptbNJ7D+H/eU=}

internal:
    # Shared secret added to every forwarded request as X-Internal-Secret header.
    # Microservices reject requests where this header is missing or wrong.
    # MUST match the value configured in all microservices. Change in production via env var.
    gateway-secret: ${GATEWAY_INTERNAL_SECRET:dktxrevDkto3aZUvUD6REkn+x2x0foPhAvXBueFO4rs=}

# Paths that do not require a valid JWT — forwarded without X-User-* headers
# Supports Ant-style patterns: * matches one segment, ** matches multiple
gateway:
    public-paths:
        - /api/auth/register
        - /api/auth/login
        - /api/auth/refresh
        - /api/auth/forgot-password
        - /api/auth/reset-password
        - /api/auth/verify-email
        - /api/auth/resend-verification
        - /api/invitations/*/preview

---
spring:
    config:
        activate:
            on-profile: dev
    data:
        redis:
            host: localhost
            port: 6380
    kafka:
        bootstrap-servers: localhost:9092
        consumer:
            group-id: api-gateway
            auto-offset-reset: earliest
            key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
            properties:
                spring.json.trusted.packages: "*"
                spring.json.value.default.type: io.bento.apigateway.event.OrgMemberStaleEvent
                spring.json.use.type.headers: "false"
    cloud:
        gateway:
            server:
                webflux:
                    routes:
                        - id: auth-service
                          uri: http://localhost:8081
                          predicates:
                              - Path=/api/auth/**, /api/users/**

                        - id: org-service
                          uri: http://localhost:8082
                          predicates:
                              - Path=/api/orgs, /api/orgs/**, /api/members/**, /api/invitations/**

                        - id: board-service
                          uri: http://localhost:8083
                          predicates:
                              - Path=/api/boards, /api/boards/**

                        - id: task-service
                          uri: http://localhost:8084
                          predicates:
                              - Path=/api/issues, /api/issues/**, /api/sprints, /api/sprints/**, /api/epics, /api/epics/**

                        - id: notification-service
                          uri: http://localhost:8085
                          predicates:
                              - Path=/api/notifications, /api/notifications/**

                        - id: realtime-service
                          uri: http://localhost:8086
                          predicates:
                              - Path=/ws/**

---
spring:
    config:
        activate:
            on-profile: docker
    data:
        redis:
            host: redis
            port: 6379
    kafka:
        bootstrap-servers: kafka:29092
        consumer:
            group-id: api-gateway
            auto-offset-reset: earliest
            key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
            properties:
                spring.json.trusted.packages: "*"
                spring.json.value.default.type: io.bento.apigateway.event.OrgMemberStaleEvent
                spring.json.use.type.headers: "false"
    cloud:
        gateway:
            server:
                webflux:
                    routes:
                        - id: auth-service
                          uri: http://auth-service:8081
                          predicates:
                              - Path=/api/auth/**, /api/users/**

                        - id: org-service
                          uri: http://org-service:8082
                          predicates:
                              - Path=/api/orgs, /api/orgs/**, /api/members/**, /api/invitations/**

                        - id: board-service
                          uri: http://board-service:8083
                          predicates:
                              - Path=/api/boards, /api/boards/**

                        - id: task-service
                          uri: http://task-service:8084
                          predicates:
                              - Path=/api/issues, /api/issues/**, /api/sprints, /api/sprints/**, /api/epics, /api/epics/**

                        - id: notification-service
                          uri: http://notification-service:8085
                          predicates:
                              - Path=/api/notifications, /api/notifications/**

                        - id: realtime-service
                          uri: http://realtime-service:8086
                          predicates:
                              - Path=/ws/**

---
spring:
    config:
        activate:
            on-profile: selfhost
    data:
        redis:
            host: ${REDIS_HOST:localhost}
            port: ${REDIS_PORT:6379}
    kafka:
        bootstrap-servers: ${KAFKA_SERVERS:localhost:9092}
        consumer:
            group-id: api-gateway
            auto-offset-reset: earliest
            key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
            properties:
                spring.json.trusted.packages: "*"
                spring.json.value.default.type: io.bento.apigateway.event.OrgMemberStaleEvent
                spring.json.use.type.headers: "false"
    cloud:
        gateway:
            server:
                webflux:
                    routes:
                        - id: auth-service
                          uri: http://${AUTH_SERVICE_HOST:auth-service}:8081
                          predicates:
                              - Path=/api/auth/**, /api/users/**

                        - id: org-service
                          uri: http://${ORG_SERVICE_HOST:org-service}:8082
                          predicates:
                              - Path=/api/orgs, /api/orgs/**, /api/members/**, /api/invitations/**

                        - id: board-service
                          uri: http://${BOARD_SERVICE_HOST:board-service}:8083
                          predicates:
                              - Path=/api/boards, /api/boards/**

                        - id: task-service
                          uri: http://${TASK_SERVICE_HOST:task-service}:8084
                          predicates:
                              - Path=/api/issues, /api/issues/**, /api/sprints, /api/sprints/**, /api/epics, /api/epics/**

                        - id: notification-service
                          uri: http://${NOTIFICATION_SERVICE_HOST:notification-service}:8085
                          predicates:
                              - Path=/api/notifications, /api/notifications/**

                        - id: realtime-service
                          uri: http://${REALTIME_SERVICE_HOST:realtime-service}:8086
                          predicates:
                              - Path=/ws/**

                        - id: realtime-service
                          uri: http://${REALTIME_SERVICE_HOST:realtime-service}:8086
                          predicates:
                              - Path=/ws/**

---
spring:
    config:
        activate:
            on-profile: cloud
    data:
        redis:
            host: ${REDIS_HOST:redis}
            port: ${REDIS_PORT:6379}
    kafka:
        bootstrap-servers: ${KAFKA_SERVERS:kafka:29092}
        consumer:
            group-id: api-gateway
            auto-offset-reset: earliest
            key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
            properties:
                spring.json.trusted.packages: "*"
                spring.json.value.default.type: io.bento.apigateway.event.OrgMemberStaleEvent
                spring.json.use.type.headers: "false"
    cloud:
        gateway:
            server:
                webflux:
                    routes:
                        - id: auth-service
                          uri: http://${AUTH_SERVICE_HOST:auth-service}:8081
                          predicates:
                              - Path=/api/auth/**, /api/users/**

                        - id: org-service
                          uri: http://${ORG_SERVICE_HOST:org-service}:8082
                          predicates:
                              - Path=/api/orgs, /api/orgs/**, /api/members/**, /api/invitations/**

                        - id: board-service
                          uri: http://${BOARD_SERVICE_HOST:board-service}:8083
                          predicates:
                              - Path=/api/boards, /api/boards/**

                        - id: task-service
                          uri: http://${TASK_SERVICE_HOST:task-service}:8084
                          predicates:
                              - Path=/api/issues, /api/issues/**, /api/sprints, /api/sprints/**, /api/epics, /api/epics/**

                        - id: notification-service
                          uri: http://${NOTIFICATION_SERVICE_HOST:notification-service}:8085
                          predicates:
                              - Path=/api/notifications, /api/notifications/**

                        - id: realtime-service
                          uri: http://${REALTIME_SERVICE_HOST:realtime-service}:8086
                          predicates:
                              - Path=/ws/**